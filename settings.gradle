pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
rootProject.name = "WkWhisperKey"
include ':android:app'
include(":core")
project(":core").projectDir = file("core")
